# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

target 'IKLogger' do
    pod 'IKCore', '~> 1.0'
	pod 'Bugsnag'
end

target 'IKLoggerTests' do
	pod 'Specta', '~> 0.5'
	pod 'Expecta', '~> 0.3'
end

